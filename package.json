{
  "name": "blog",
  "private": true,
  "version": "1.0.0",
  "description": "Daily blog optimized for humans and LLMs",
  "scripts": {
    "validate": "node scripts/validate-frontmatter.mjs",
    "build:index": "node scripts/generate-index.mjs",
    "build:site": "node scripts/build-site.mjs",
    "build": "npm run validate && npm run build:index && npm run build:site",
    "new:daily": "node scripts/new-daily-post.mjs"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^17.0.3"
  }
}
